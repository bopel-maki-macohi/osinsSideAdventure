package flxnovel.data.visualnovel.background;

enum abstract BackgroundPropCallback(String) from String to String
{
	/**
	 * Alias for centerXY
	 */
	var center = 'center';

	var centerXY = 'centerXY';
	var centerX = 'centerX';
	var centerY = 'centerY';
}
